package generator;

/**
 * the interface provides the single method nextInt, which should return
 * random int numbers according to the concrete implementation
 */

public interface RandomRule {
    int nextInt();
}
