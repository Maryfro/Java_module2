class MyThread extends Thread {

    MyThread myThread = new MyThread();


    // how to open a thread
    //use class Thread
    //Class MyThread extends Thread
    //@Override method public void run()
    //{
    // write the logic of new Thread here
    // }
    /*
    MyThread myThread = new MyThread;
    myThread.start();
    if we have MyThread.run(), new thread does not open
    and runs in the old one
    */
}